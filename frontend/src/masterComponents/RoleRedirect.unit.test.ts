import { describe, it, expect } from 'vitest';
import { RoleRedirect } from './RoleRedirectM';

describe('RoleRedirect', () => {
  it('is a function', () => {
    expect(typeof RoleRedirect).toBe('function');
  });
});