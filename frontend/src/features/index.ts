// ── Feature modules ──
// Role-scoped screens, lazy-loaded via React.lazy().
// Each feature lives in its own subdirectory.
// Features can import from: application/, infra/, domain/, shared/
// Features must NOT import from other features.