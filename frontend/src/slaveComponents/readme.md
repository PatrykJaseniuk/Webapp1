# Slave Components
- .tsx components
- pure functions
- components not contain state
- Gui definition
- all data and actions passed by props
- no data fetch (pure functions, no state)
- components have 0 knowledge about rest of app (forEx url paths)
- every slave component have its own master. Propetry type of slave is define in master component  
